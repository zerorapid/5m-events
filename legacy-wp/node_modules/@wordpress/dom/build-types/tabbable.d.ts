/**
 * Returns true if the specified element is tabbable, or false otherwise.
 *
 * @param {Element} element Element to test.
 *
 * @return {boolean} Whether element is tabbable.
 */
export declare function isTabbableIndex(element: Element): boolean;
export type MaybeHTMLInputElement = HTMLElement & {
    type?: string;
    checked?: boolean;
    name?: string;
};
/**
 * @param {Element} context
 * @return {HTMLElement[]} Tabbable elements within the context.
 */
export declare function find(context: Element): HTMLElement[];
/**
 * Given a focusable element, find the preceding tabbable element.
 *
 * @param {Element} element The focusable element before which to look. Defaults
 *                          to the active element.
 *
 * @return {HTMLElement|undefined} Preceding tabbable element.
 */
export declare function findPrevious(element: Element): HTMLElement | undefined;
/**
 * Given a focusable element, find the next tabbable element.
 *
 * @param {Element} element The focusable element after which to look. Defaults
 *                          to the active element.
 *
 * @return {HTMLElement|undefined} Next tabbable element.
 */
export declare function findNext(element: Element): HTMLElement | undefined;
//# sourceMappingURL=tabbable.d.ts.map