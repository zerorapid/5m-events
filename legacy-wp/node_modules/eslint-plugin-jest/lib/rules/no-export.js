"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _utils = require("@typescript-eslint/utils");
var _utils2 = require("./utils");
var _default = exports.default = (0, _utils2.createRule)({
  name: __filename,
  meta: {
    docs: {
      description: 'Disallow using `exports` in files containing tests'
    },
    messages: {
      unexpectedExport: `Do not export from a test file`
    },
    type: 'suggestion',
    schema: []
  },
  defaultOptions: [],
  create(context) {
    const exportNodes = [];
    let hasTestCase = false;
    return {
      'Program:exit'() {
        if (hasTestCase) {
          for (const node of exportNodes) {
            context.report({
              node,
              messageId: 'unexpectedExport'
            });
          }
        }
      },
      CallExpression(node) {
        if ((0, _utils2.isTypeOfJestFnCall)(node, context, ['describe', 'test'])) {
          hasTestCase = true;
        }
      },
      'ExportNamedDeclaration, ExportDefaultDeclaration, TSExportAssignment'(node) {
        exportNodes.push(node);
      },
      'AssignmentExpression > MemberExpression'(node) {
        let {
          object,
          property
        } = node;
        while (object.type === _utils.AST_NODE_TYPES.MemberExpression) {
          ({
            object,
            property
          } = object);
        }
        if (object.type !== _utils.AST_NODE_TYPES.Identifier) {
          return;
        }
        if ((object.name !== 'module' || (0, _utils2.resolveScope)(context.sourceCode.getScope(object), 'module') !== null) && (object.name !== 'exports' || (0, _utils2.resolveScope)(context.sourceCode.getScope(object), 'exports') !== null)) {
          return;
        }
        if (object.name === 'exports' || (0, _utils2.isSupportedAccessor)(property, 'exports') || (0, _utils2.isSupportedAccessor)(property, 'export')) {
          exportNodes.push(node);
        }
      }
    };
  }
});